{
  "name": "marine-workspace",
  "private": true,
  "scripts": {
    "dev": "npx pnpm -r --parallel dev",
    "build": "npx pnpm -r build",
    "start:engine": "npx pnpm -C ingestion-engine start",
    "dev:frontend": "npx pnpm -C frontend dev"
  }
}
